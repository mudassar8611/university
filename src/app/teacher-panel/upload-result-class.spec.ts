import { UploadResultClass } from './upload-result-class';

describe('UploadResultClass', () => {
  it('should create an instance', () => {
    expect(new UploadResultClass()).toBeTruthy();
  });
});
